/**
 * Re-export from central types with legacy aliases for backward compat.
 */
export type {
  ContentElement as PublicElement,
  ContentPost as PublicPost,
  ContentFaq as PublicFaq,
  WordDefinition as PublicWordDefinition,
  ContentWord as PublicWord,
  ContentDictionary as PublicDictionary,
} from '@/types/dictionary'
