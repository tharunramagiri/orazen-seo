// Empty stub that stands in for `server-only` when running scripts outside the
// Next bundler. Next's real `server-only` throws on client-side imports; for
// server-side script execution it is effectively a no-op.
module.exports = {}
